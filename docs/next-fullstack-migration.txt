Essentialist — Next.js full-stack migration (**runtime API complete**)

Status (2026):
  - **Production API** is served by **Next Route Handlers** under **`/api/next/*`**
    (`client/src/fullstack/` + `client/src/app/api/next/`).
  - **Browser HTTP** uses **`@/lib/apiClient`** (axios instance in `client/src/utils/Axios.js`:
    SummaryApi baseURL, cookies, 401 refresh). Do not import `@/backend/http/legacyClient` (removed).
  - **`server/` (Express)** remains in the repo for **reference, scripts, or optional sidecars**;
    the Next app does **not** require Express for store API traffic when env points at Next.

Are we moving `server/` (Express) into Next.js?
  Original plan was **dual track** while routes were ported:
  - **`server/`** — Express + Mongo + routes (unchanged on disk; see server/MIGRATION.txt).
  - **`client/src/fullstack/`** — Mongoose models, controllers, Route Handlers vs **same `MONGODB_URI`**.
  - **`client/src/backend/`** — contracts (`summaryApi`), optional **Express probe** (`legacyExpressGateway.js`).

Git branch (workspace):
  feat/next-fullstack-migration

If you connect a remote after a fresh `git init` in this workspace:
  git remote add origin <url>
  git fetch
  (merge or rebase as needed — history may not match an older remote)

Optional Express (`server/`):
  server/ — Express + Mongo + routes and jobs (see server/MIGRATION.txt)

Frontend — backend integration surface:
  client/src/backend/
    config/runtimePublicEnv.js     — env accessors
    config/upstreamOrigin.js       — getLegacyExpressOrigin() for optional ops probes only
    http/axiosToastError.js        — REMOVED; use @/lib/axiosToastError
    contracts/summaryApi.js      — re-export of src/common/SummaryApi.js
    services/legacyExpressGateway.js — optional fetch to Express (e.g. LEGACY_EXPRESS_HEALTH_PROBE)
    index.js                       — barrel exports (httpClient → ../lib/apiClient.js)

Browser HTTP (canonical):
  client/src/lib/apiClient.js          — re-export of utils/Axios.js
  client/src/lib/axiosToastError.js    — re-export of utils/AxiosToastError.js

Path aliases (client/jsconfig.json):
  @/*         → client/src/*
  @backend/*  → client/src/backend/*
  @fullstack/* → client/src/fullstack/*

Next-native full-stack (implements API in Next; `server/` not imported or modified):
  client/src/fullstack/
    db/mongoose.js
    schemas/auth.schema.js
    models/ — user, cartproduct, address, order, category, subCategory, product, brand,
      blog, rating, review, review_comment
    lib/ — localization, jwtTokens, invokeController (rate limits + optional idempotency +
      duplicate-order for order/Payunit POSTs; `rateLimiterMemory.js`), nextRoute (auth, admin,
      optional auth, admin privilege), cacheNoop,
      mail (from server utils), uploadImageClodinary,
      requireAdminPrivilege (ADMIN | SUPER_ADMIN), authFromRequest (cookie + Bearer)
    config/ — sendEmail.js, stripe.js (lazy: null if STRIPE_SECRET_KEY unset)
    controllers/ — user, cart, address, category, subcategory, product, brand, order,
      payments (Payunit), guestadmin, admin (stats), rating, review, blog, indexnow (handlers
      copied from server; imports retargeted)
  Route Handlers under /api/next/ (prefix `next` avoids colliding with /api/user):
    /api/next/user/*     — full auth surface + POST merge-guest-data (guest cart/addresses/orders),
      PUT upload-avatar (multipart field `avatar`)
    /api/next/cart/*     — create, get, update-qty, delete-cart-item (Bearer auth)
    /api/next/address/*  — create, get, update, disable (Bearer auth)
    /api/next/category/* — add-category (admin), get, update (admin), delete (admin)
    /api/next/subcategory/* — create (admin), get (POST public), update, delete (admin)
    /api/next/product/*  — all Express product POST/PUT/DELETE routes
    /api/next/brand/*    — create (admin), list (GET), [identifier] (GET), update/[id], delete/[id]
    /api/next/order/*    — verify-receipt (POST), cash-on-delivery (POST auth), guest-cod (POST),
      checkout (POST auth), webhook (POST raw Stripe signature), order-list (GET auth),
      receipt/[orderId] (GET optional auth + cookie/Bearer like Express)
    /api/next/payments/* — Payunit (same behavior as server/route/payments.js): POST mtn,
      orange, guest-mtn, guest-orange (idempotency + **payments** rate limit); POST webhook
      (**webhooks** limit); GET status/[transactionId], order-status/[orderId], guest-order-status,
      invoice/[invoiceId] (**payments** limit); POST invoice; POST disburse. Webhook notify URL defaults
      to `${BACKEND_URL}/api/next/payments/webhook` when `BACKEND_URL` is set, else
      `${FRONTEND_URL}/api/next/payments/webhook`; override with `PAYUNIT_NOTIFY_URL`.
    /api/next/admin/*    — GET dashboard = **admin.controller** stats; GET summary =
      guest-style totals (same handler family as legacy guestadmin dashboard payload); orders,
      guest-orders, PATCH orders/[orderId]/deliver
    /api/next/indexnow/* — submit-url, submit-urls, notify-content-change (POST), key (GET),
      regenerate-key (POST); same env as Express (`SITE_HOST`, `PUBLIC_DIR`, `INDEX_NOW_KEY`)
    /api/next/ratings/*  — POST /ratings (optional auth), GET|DELETE /ratings/[productId]
    /api/next/reviews/*  — public list routes, admin moderation, comments (see path notes below)
    /api/next/blog/*     — create/update/delete (admin), list & [slug] (optional auth)
    /api/next/file/upload — POST multipart field `image` (Bearer auth), Cloudinary same env as Express

  Reviews path notes (Next cannot mount two sibling `[id]` dynamic segments; a few URLs differ):
    - Product-scoped list + delete-my-review: GET|DELETE /api/next/reviews/product/[productId]
      (Express: GET|DELETE /api/reviews/:productId)
    - Comments: GET|POST /api/next/reviews/review/[reviewId]/comments
      (Express: /api/reviews/:reviewId/comments)
    - Other paths match Express under /reviews/... (public, public/product/..., id/..., admin/..., comments/...).

  Cutover: `SummaryApi` and Axios now target **`/api/next/...`** on the same
  origin. Set `NEXT_PUBLIC_API_URL` and `API_URL` to your Next origin (e.g.
  `http://localhost:3000`) so server components and `next build` self-fetch hit
  Route Handlers, not Express. Stripe webhooks must use **`/api/next/order/webhook`**.
  Guest receipt GET: **`SummaryApi.guestOrderReceipt(orderId, integrityToken)`** maps to
  **`GET /api/next/order/receipt/:orderId?token=...`** (same as `downloadReceipt` for guests).

  Env: same MongoDB + JWT secrets as Express; `requireAdmin` routes need role ADMIN.
  Review admin routes use `requireAdminPrivilege` (ADMIN or SUPER_ADMIN). Auth reads
  `accessToken` cookie first, then `Authorization: Bearer` (matches Express `auth`).
  Payunit notify URL: set `PAYUNIT_NOTIFY_URL` explicitly, or rely on `BACKEND_URL` /
  `FRONTEND_URL` (see `/api/next/payments/*` note above).

  Note: Express Redis cache middleware is a no-op in Next (`cacheNoop.js`). **Multer** is not
  wired on Next (uploads use Next multipart handling on specific routes). Order placement and
  Payunit POST routes use **in-memory rate limits** matching Express defaults (`ordersLimiter` /
  `paymentsLimiter`); Stripe and Payunit webhooks use **`webhooks`** limits (same windows as
  Express). Those routes also require **`X-Idempotency-Key`** where Express does, with the same
  in-memory idempotency behavior as Express when Redis is unavailable; COD routes run the same
  **duplicate-order** fingerprint check. Redis-backed idempotency / rate limits across multiple
  Next instances remain optional.

  Stripe: `fullstack/config/stripe.js` uses `new Stripe(secret)` (Stripe SDK v22). Webhook
  route verifies signature with raw body then calls the same `webhookStripe` handler.

  Payunit: `fullstack/config/payunit.js` exposes `getPayunitClient()` (lazy; returns `null`
  if `PAYUNIT_API_KEY`, `PAYUNIT_API_USERNAME`, and `PAYUNIT_API_PASSWORD` are unset so
  `next build` does not throw). Optional `PAYUNIT_MODE` (default `test`). Install
  `@payunit/nodejs-sdk` in the client app.

Ops / overlap:
  client/src/app/api/system/migration-status/route.js — Next JSON probe (`migration: next_api_complete`)
  client/src/app/api/legacy/health/route.js — Next-only by default; set `LEGACY_EXPRESS_HEALTH_PROBE=true`
    and `API_URL` / `NEXT_PUBLIC_API_URL` to Express origin to include upstream GET /health

Import conventions (app code):
  - Browser: **`import apiClient from "@/lib/apiClient"`** (default export = axios), or named `apiClient`.
  - Contracts: `@/backend/contracts/summaryApi` or `@/common/SummaryApi`.
  - Toasts on axios errors: **`@/lib/axiosToastError`** (or `@/utils/AxiosToastError`).
  - Intentional: `client/src/utils/Axios.js` imports `@/backend/contracts/summaryApi` for baseURL + refresh routes
    (re-export of `common/SummaryApi`; avoids duplicate path conventions).
  - utils/indexnow.js and hooks/useIndexNow.js use `@/backend/contracts/summaryApi`.

Live imports of `@/utils/Axios` outside `utils/Axios.js`: discouraged; prefer `@/lib/apiClient`.

Conventions:
  - Optional Express probes: `@/backend/services/legacyExpressGateway` (server-side only).
  - Keep `client/src/common/SummaryApi.js` and `client/src/utils/Axios.js`; refresh logic lives there.

Completed checklist:
  - All app modules import **`@/lib/apiClient`** instead of **`@/backend/http/legacyClient`**.
  - Toast helper imports use **`@/lib/axiosToastError`** (removed `backend/http/axiosToastError.js`).
  - **`/api/system/migration-status`** reports **`next_api_complete`**.
  - **`/api/legacy/health`** does not call Express unless **`LEGACY_EXPRESS_HEALTH_PROBE=true`**.
